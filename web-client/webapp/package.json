{
  "name": "web-client",
  "version": "1.0.0",
  "description": "Default web-client for space-expansion",
  "main": "typescript/app.ts",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "compile": "tsc",
    "build_dev": "npm run compile && webpack --mode development",
    "build": "npm run compile &&  webpack --mode production"
  },
  "dependencies": {
    "konva": "^8.3.12"
  },
  "devDependencies": {
    "typescript": "^4.8.2",
    "ts-loader": "^9.3.1",
    "webpack": "^5.74.0",
    "webpack-cli": "^4.10.0"
  }
}
