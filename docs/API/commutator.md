# ICommutator - интерфейс для подключения к устройствам

Previous topic: [ISessionControl](./sessions-control.md)

<!-- После успешной авторизации клиент получает в сообщении `access_granted` как
UDP-порт, так и **идентификатор корневой сессии** `session_id`. Корневая
сессия реализует интерфейс `IRootSession`. Единственное её назначение - это
порождать новые сессии для подключения к корневому коммутатору игрока.
Что такое коммутатор мы обсудим в следующей главе. -->